<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <meta http-equiv="X-UA-Compatible" content="ie=edge" />
        <title>J&M Angeln - Merch</title>

        <link rel="stylesheet" href="/styles/global.css">
        <link rel="stylesheet" href="/styles/info-card.css">
        <link rel="stylesheet" href="/styles/cards.css">
        <link rel="stylesheet" href="/styles/merch.css" />

    </head>
    <body>

        <% include includes/navigation %>

        <main>

            <% include includes/slideshow %>

            <div class="heading"><h1>Merchandise</h1></div>

            <!-- <section class="filter">
            <input id="search" type="text" id="search" name="search" placeholder="Suchen...">
            <div class="price__config">
                <div class="price__range">
                    <span>1 €</span>
                    <input id="price" type="range" name="price" min="1" max="50" value="50" step="1">
                    <span>50 €</span>
                </div>
                <p class="price__output">Alle Preise</p>
            </div>
            <div class="categories">
                <h3 class="active">Alle</h3>
                <h3>T-Shirts</h3>
                <h3>Hoodies</h3>
                <h3>Caps</h3>
            </div>
        </section> -->

            <section class="products cards">
                <div class="card-wrapper">
                    <% for (const product of products) { %>
                        <a
                            class="product card"
                            href="/merch/produkt/<%= product._id %>"
                            data-name="<%= product.name %>"
                            data-price="<%= (product.price).toFixed(2) %>"
                        >
                            <img
                                class="lazy card__image"
                                src="<%= product.images[0] %>"
                                alt="product-card image"
                            >
                            <div class="card__content">
                                <div class="product__info">
                                    <h1><%= product.name %></h1>
                                    <h1 class="price">
                                        <%= (product.price).toFixed(2).replace('.', ',') %> €
                                    </h1>
                                </div>
                            </div>
                        </a>
                    <% } %>
                </div>
            </section>
        </main>

        <% include includes/footer %>

        <!-- <cart-bar qty="<%= quantity %>"></cart-bar> -->

        <!-- <script>
            const soldOutProducts = document.querySelectorAll('.soldout');
            for (const product of soldOutProducts) {
              const sign = document.createElement('h1');
              sign.textContent = 'Ausverkauft';
              sign.style.cssText = 'text-transform:uppercase;color:red;font-size:24px';
              product.firstElementChild.children[1].replaceWith(sign);
              product.setAttribute('disabled', true);
            }
        </script> -->
    </body>
</html>
